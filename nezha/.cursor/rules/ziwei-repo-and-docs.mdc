---
description: 紫微主仓与子项目 Git、文档归属及 BMAD 产出约定；规约对 Cursor / Claude Code CLI / Codex 均生效
alwaysApply: true
---

# 统一 Agent 规约（Cursor 加载入口）

本仓库使用**根目录统一 Agent 文档**，无需每次指定路径。

- **必读**：在会话中始终加载并遵守 **根目录 `AGENTS.md`**。该文件为本仓库对所有 Agent（Cursor、Claude Code CLI、Codex）的统一规约入口。
- **说明**：多 IDE 约定见 `docs/open/technical/规约与多IDE约定.md`。

请先读取项目根目录 **AGENTS.md** 的完整内容，并在后续所有回复与修改中遵循其中约定。

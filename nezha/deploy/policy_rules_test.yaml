# 测试用策略规则：所有 exec:run 需要审批
rules:
  - id: review_all_exec
    action: "exec:run"
    resource: "*"
    decision: review
    reason: 测试环境需要审批

  - id: allow_read
    action: GET
    resource: "*"
    decision: allow
    reason: 只读操作放行

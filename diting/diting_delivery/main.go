// diting-delivery：投递（飞书等）入口（I-014 多容器形态占位）。
// 当前推荐使用 cmd/diting_allinone 一体部署；本入口为未来拆分为独立投递服务预留。
package main

import "fmt"

func main() {
	fmt.Println("diting-delivery: placeholder for multi-container mode. Use cmd/diting_allinone for full stack.")
}

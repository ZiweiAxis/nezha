# 项目规约（Claude Code CLI 加载入口）

本仓库使用**根目录统一 Agent 文档**，无需每次指定其他路径。

**必读**：在会话开始或执行任务前，请先读取并遵守 **根目录 `AGENTS.md`**。该文件为本仓库对所有 Agent（Cursor、Claude Code CLI、Codex）的统一规约，包含主仓与子项目、Git、BMAD、文档归属、Agent 调度等全部约定。

请先加载根目录 **AGENTS.md** 并在此会话中始终遵循其约定。
